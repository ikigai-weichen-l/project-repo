apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-app-dev-ap-southeast-1
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: my-app:dev
spec:
  project: default
  sources:
    - repoURL: https://github.com/YOUR_ORG/central-helm-repo
      targetRevision: main
      path: charts/app-template
      helm:
        valueFiles:
          - $values/envs/dev/ap-southeast-1/values.yaml

    - repoURL: https://github.com/YOUR_ORG/project-repo
      targetRevision: main
      ref: values

  destination:
    server: https://k8s-dev-ap-southeast-1.example.com
    namespace: my-app-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
