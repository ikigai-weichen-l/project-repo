apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: my-app-shared-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: demo-test-secret-store
    kind: ClusterSecretStore
  target:
    name: my-app-shared-secret
    creationPolicy: Owner
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: DATABASE_URL
